# 部署与监控示例

本目录提供部署边界说明，应用代码示例位于 `../graceful` 和 `../observability`。

生产环境至少应补充：

- 固定基础镜像版本，并以非 root 用户运行；
- 使用 Secret 管理数据库密码和 JWT 密钥；
- 配置健康检查、优雅关闭和启动探针；
- 为日志、指标和追踪设置采集与告警；
- 对数据库迁移、镜像扫描和回滚流程做自动化验证。
